import 'package:flutter/material.dart';
import 'package:team_inside/presentation/core/widgets/core_select_image_dialog.dart';

class TeamsFrameworkSelectImageDialog extends StatelessWidget {
  const TeamsFrameworkSelectImageDialog({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return CoreSelectImageDialog();
  }
}
